# URL to list works

    query <- list(
      project       = "CMIP6",
      type          = "Dataset",
      latest        = "true",
      frequency     = "mon",
      variable_id   = "co2mass",
      experiment_id = "historical"
    )

